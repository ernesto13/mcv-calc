$(document).ready(function($) {
	
    let calcBtn = $('#calc-btn');
    let hgb = $('#');
    let hct = $('#hct');
    let rbc = $('#rbc');
    
    calcBtn.on('click', (e) => {
        e.preventDefault();

    });
});